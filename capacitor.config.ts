import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.APP_PLACEHOLDER_NAME.app',
  appName: 'APP_PLACEHOLDER_NAME',
  webDir: 'dist/APP_PLACEHOLDER_NAME/browser'
};

export default config;
